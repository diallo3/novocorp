'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var removeTemplates = function removeTemplates(args) {
  var type = args[0];
  var name = args[1];

  _fs2.default.unlink('./' + type + '-' + name + '.php', function (err) {
    if (err) {
      console.log(err);
    } else {
      console.log('PHP Template Removed');
    }
  });
  _fs2.default.unlink('./views/' + type + 's/' + type + '-' + name + '.twig', function (err) {
    if (err) {
      console.log(err);
    } else {
      console.log('Twig Template Removed');
    }
  });
};

exports.default = removeTemplates;