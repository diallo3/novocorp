'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _mkdirp = require('mkdirp');

var _mkdirp2 = _interopRequireDefault(_mkdirp);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var createTemplates = function createTemplates(args) {
  var type = args[0];
  var name = args[1];

  _fs2.default.writeFileSync(type + '-' + name + '.php', buildPHPTemplate(type, name));
  _mkdirp2.default.sync('./views/' + type + 's');
  _fs2.default.writeFileSync('./views/' + type + 's/' + type + '-' + name + '.twig');

  console.log('Template Created!');
};

function buildPHPTemplate(type, name) {
  return '<?php \n' + '\n' + '$context = Timber::get_context(); \n' + '$post = new TimberPost(); \n' + '$context["post"] = $post; \n' + 'Timber::render("/views/' + type + 's/' + type + '-' + name + '.twig", $context); \n' + '\n' + '?>';
}

exports.default = createTemplates;